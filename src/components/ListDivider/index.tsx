import React, { ReactElement } from 'react';
import { View } from 'react-native';
import { styles } from './styles';

export default function listHeader(): ReactElement {
  return (
    <View style={styles.container} />
  );
}